# Recoverable backups sync.sh makes when it replaces a real file with a symlink.
backups/

# Secrets / tokens — never commit.
*.token
auth-token
.env
*.pem

# Any credential a skill or tool of yours drops here. Broad on purpose: the
# failure mode is a token committed once and public forever.
secrets/
*secret*.json
