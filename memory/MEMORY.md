# Memory index, user scope (all agents)

Read this first; it indexes the rest. One line per memory. Cross-agent: lives in `~/.agents/memory/`.

> This ships as a template. The `onboarding` skill seeds your first entries. Add a line here for every
> memory file you create (use the `remember` skill). One line each, no content, the file holds the fact.

- [User Profile](user_profile.md), who you are: background, role, preferences (onboarding fills this)
- [Voice Profile](voice_profile.md), how you actually sound: measured register, tics, and the log of edits you made to agent drafts (the `voice` skill fills this)
